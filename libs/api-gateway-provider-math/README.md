# api-gateway-provider-math

This library was generated with [Nx](https://nx.dev).

## Running unit tests

Run `nx test api-gateway-provider-math` to execute the unit tests via [Jest](https://jestjs.io).
